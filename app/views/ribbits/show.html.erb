<h1><%= @ribbit.content %></h1>
<div id="like_form">
	<% if current_user.like?(@ribbit) %>
		<%= render 'unlike', ribbit: @ribbit %>
	<% else %>
		<%= render 'like', ribbit: @ribbit %>
	<% end %>
</div>